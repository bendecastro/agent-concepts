# Gate A result — 2026-07-26 (v3 review economy)

Result: **PASS — 21/21**

Runner: [run-pressure.py](../run-pressure.py). Sandbox: `/tmp/bc-drain-v2-gate-a-c66aygkn`
(retained; `SANDBOX-READY` marker present). Base SHA `8df2b36d6b17c8892515fcb932870ce494f099ee`,
changed base `f24aea876cfd04c72d423377ac1e99f9fd3d60bb`. No network; every push/`gh`/publish
attempt resolved to PATH-first stubs against a local disposable bare remote.

Checks 1–17 are the v2 contract, re-run unchanged. Checks 18–21 are new.

## New coverage

**18 — materialized packet, no re-derivation.** A real round-1 packet was built from a real
`git diff` in a dedicated worktree. Its `diff_sha256` (`96ea2669b250…`) was asserted equal to a
freshly recomputed hash of the reviewed diff bytes, the five round-1 entries were asserted exact,
and the packet path was asserted outside every worktree. Reviewer command logs were scanned for
`git status`/`git diff`/`ls-files` re-derivation; none present, context reads permitted.

**19 — standing approval and selective re-review.** All thirteen invalidation cases were
evaluated through the runner's deterministic `invalidate()` function: the untouched approving axis
is not re-dispatched; each Spec trigger (matrix-mapped file, public interface, observable
behavior, external platform), each Standards trigger (file added/removed, dependencies, privilege,
launcher topology, test changed), and each both-axis trigger (base changed, unmappable delta)
dispatches as specified. A real second-round edit changed the diff hash; landing was asserted to
reject a stale standing approval and a single-axis approval, and to accept only both axes bound
to the final hash.

**20 — review tiers and one-way escalation.** Eight cases: ordinary → tier 1; high-risk, public
interface, privilege, and launcher-topology diffs → tier 2; gate rejection → tier 2; tier-1
Critical finding → tier 2; and an already-escalated issue never lowers. The tier-1 output schema
was asserted to carry exactly `verdict`/`findings`/`axes_covered` with `axis` on each finding.

**21 — gated reproduction budget.** An instrumented reviewer command log was asserted to contain
no reproduction command before its finding was named, at most two per finding, and no full-suite
invocation; a refuted hypothesis produced no finding and no prose.

## Negative control

The v3 runner was executed against a `git worktree` of the pre-v3 canon (v2 bodies, v3 runner). It
failed at the first v3 assertion — `review contract missing do not re-derive it` — confirming the
canon-text assertions bite rather than passing vacuously.

## Limitation

This runner is an executable spec of the driver's deterministic rules. It proves those rules are
encoded, mutually consistent, and present in canon; it does not prove a model obeys them under
load. That is Gate B's job, and **v3 Gate B has not been run**. It must use a contested fixture
forcing at least two rework rounds — a clean fixture never exercises selective re-review, which is
the change's main effect.

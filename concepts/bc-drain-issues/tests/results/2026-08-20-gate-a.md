# Gate A — 2026-08-20 — PASS 23/23

Run after adding the discriminating-acceptance-evidence rule to
`body/review-contract.md` and the matching clause to `body/execute-issue.md`.

- Runner: `run-pressure.py`
- Baseline before the edits, same runner and shim: **PASS 22/22**
  (sandbox `/tmp/bc-drain-v2-gate-a-tloey4oc`), so the result is attributable.
- After the edits: **PASS 23/23**, sandbox `/tmp/bc-drain-v2-gate-a-uqb95_b9`,
  `summary.json` reports `all_23_pass: true`, `no_real_mutation: true`.
- Fireability check: deleting the rule paragraph from `review-contract.md`
  fails check 23 with `review contract missing discriminating-evidence rule`;
  restoring it passes. The check is not inert.

## Harness portability finding (pre-existing, not fixed here)

`run-pressure.py` resolves `CANDIDATE` as `parents[4]` of the script and
expects `<candidate>/agents/concepts/bc-drain-issues` alongside
`<candidate>/.pi/agent/agents/`. Those were siblings when this workspace lived
in CONFIG. The workspace now sits at `Work/PUBLIC/Agents` while the Pi role
files remain in CONFIG, so the default invocation aborts with
`missing Pi role bc-drain-auditor` at HEAD, before any edit.

Both runs above used a throwaway shim directory containing two symlinks
(`agents` → this workspace, `.pi` → the CONFIG Pi directory) passed as
`argv[1]`, so the harness itself was not modified. A durable fix would resolve
the concept directory from the script location and take the role directory as
an explicit argument, but that is a portability decision affecting a
cross-repository path and was left for the user.

Gate B: NOT RUN, unchanged.

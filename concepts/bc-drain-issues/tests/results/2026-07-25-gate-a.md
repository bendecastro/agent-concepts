# Gate A result — 2026-07-25

Result: **PASS (17/17)**

Reproducible command from the CONFIG repository root:

```bash
env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY \
  -u http_proxy -u https_proxy -u all_proxy \
  python3 agents/concepts/bc-drain-issues/tests/run-pressure.py
```

The durable runner creates a fresh disposable sandbox, local bare remote, PATH-first `git`/`gh`/publish stubs, external worktrees/state, and machine-readable artifacts. It performs real local Git, three-way application, canonical Git tree-OID recovery round trips, and deterministic state/token simulations; it never performs a real push or GitHub mutation.

Passing run used for this record: `/tmp/bc-drain-v2-gate-a-afmi2bnj`.

Verified evidence:

- `artifacts/checks.json`: all 17 checks `PASS`.
- `artifacts/00/no-real-mutation.json`: disposable local remote, no network commands, every external mutation resolved to a stub.
- `artifacts/02/claim-dependency-isolation.json`: real distinct claim commit, one winning no-force claim, zero loser child tokens, and observed classify → claim → worktree → audit-dispatch order.
- `artifacts/03/audit-role-output.json`: hidden public-interface inventory, confirmed post-claim/worktree dispatch, hostile cwd/module shadow, installed-symlink topology, and primary external semantics overriding misleading repository prose.
- `artifacts/06/pi-role-and-harness-controls.json`: all three minimal Pi roles, explicit worker-discipline selection, claim-before-child-dispatch ordering, transient-environment taxonomy, four-turn/12-tool read-only bounds, nonblocking absent plan/progress files, and external/disabled artifacts including resumes.
- `artifacts/09/state-taxonomy.json`: exercised transient tool/base/environment routing never applies `needs-human`; repeated failures stop the run as `SYSTEMIC_FAILURE` while preserving issue work as deferred.
- `artifacts/12/recovery-verification.json`: six-file bundle and exact matching-base tree OID `bd9b3605646d9e63909884106fd9b9c53f0b1f91`.
- `artifacts/summary.json`: aggregate 17/17 and no-real-mutation result tied to the candidate path.

Supporting checks: `python3 agents/scripts/lint.py` exited 0 with only pre-existing deployment-symlink warnings; `git diff --check` passed.

Limitations: external-platform evidence uses a deterministic local primary-help fixture, not a live service mutation. Real-model behavior and token accounting are covered separately by Gate B.

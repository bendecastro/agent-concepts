# Gate B result — 2026-07-25

Result: **PASS**

## Locked comparison

- Identical fixture/base: `fe43c3fc3e68fba08f84e635d42d0e231a44ee77`
- Model: `openai-codex/gpt-5.6-sol`
- Thinking: explicit `medium`
- Child metric: sum of `message.usage.totalTokens` once per unique Pi child session; parent excluded
- No network, push, claim, label, close, or real GitHub mutation

| Variant | Child tokens | Cost | Outcome |
|---|---:|---:|---|
| Historical v1b | 315,474 | $0.791866 | Both reviews approved; final baseline matched |
| Candidate v2d | 277,012 | $0.920646 | Both reviews approved; final baseline matched |

Candidate token delta: **−38,462 (12.2% fewer)**. It passed the ≤500k deployment gate and stayed below the provisional 300k phase boundary. Cost increased by $0.128780, so this run establishes a token win, not a cost win.

## Candidate phases

| Phase | Tokens | Cumulative |
|---|---:|---:|
| High-risk contract audit | 12,583 | 12,583 |
| Initial worker | 152,992 | 165,575 |
| Fresh deterministic-gate rework | 50,425 | 216,000 |
| Spec review | 24,104 | 240,104 |
| Standards review + lifecycle-only resume | 36,908 | 277,012 |

The pre-code audit inventoried hidden `restart`, `reload`, and `apply-change` compatibility plus hostile-cwd/module-shadow and installed-symlink launcher behavior. The deterministic gate found stale timer contract prose/tests omitted from the worker's selected command. It preserved the same worktree and launched one fresh compact reworker. After the 200k soft crossing, focused independent Spec and Standards packets both approved with empty findings.

## Correctness

- Cached base full suite: 6 passed, one intentional failure.
- Candidate final full suite: 34 passed, the same sole failure.
- Sole failure: `tests/test_known_baseline.py::test_sushi_ordering`.
- Targeted gate after rework: 34 passed.
- No fixable work was discarded or labeled `needs-human`.
- No hard-cap defer occurred; Gate A separately validates safe phase-boundary deferral and exact recovery.

Machine-readable totals: [2026-07-25-gate-b-tokens.json](2026-07-25-gate-b-tokens.json).

## Measurement notes

Earlier candidate iterations were rejected rather than retained: they exposed generic reviewer artifact assumptions, trusted-but-wrong repository platform prose, in-worktree harness artifacts, oversized inherited role contexts, and launcher symlink topology. The final candidate incorporates those generalized fixes. Session-level source artifacts were machine-local; this durable record preserves the exact role totals, fixture/base/model, correctness outputs, and limitations, while `tests/run-pressure.py` provides reproducible deterministic coverage for the non-model invariants.
